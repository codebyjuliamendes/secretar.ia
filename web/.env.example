# URL do backend FastAPI, usada APENAS no servidor Next.js (BFF). Nunca exposta ao navegador.
API_URL="http://localhost:8000"

# Cookies de sessão: "true" em producao (HTTPS). Em desenvolvimento local pode ficar "false".
COOKIE_SECURE="false"
